/**
 * Represents the absence of value, also used to clean references.
 */
export const VOID: unique symbol = Symbol("void");

/**
 * Represents an absence of value.
 */
export type Void = typeof VOID;
